# Simple Express Server

Server written in JavaScript; cause it's Simple skills Saturday 🤷‍♂️

### To run the server:

```sh
git clone [repo name]
npm install
nodemon server.js
```

### To run the tests:

```sh
npm install
npm run test
```